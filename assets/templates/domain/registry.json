{
  "domains": {
    "example-dev-project": {
      "type": "dev-project",
      "cadenceDays": 7,
      "staleAfterDays": 60,
      "description": "Example dev project domain — replace or remove"
    },
    "example-cron-task": {
      "type": "cron-task",
      "cadenceDays": 3,
      "staleAfterDays": 30,
      "description": "Example cron task domain — replace or remove"
    },
    "example-topic-thread": {
      "type": "topic-thread",
      "cadenceDays": 2,
      "staleAfterDays": 60,
      "description": "Example topic thread domain — replace or remove"
    }
  }
}
